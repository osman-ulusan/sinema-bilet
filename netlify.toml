[build]
  command = "npm install aggregate-error p-map cacache terser-webpack-plugin@4.2.3 copy-descriptor object-copy --legacy-peer-deps && CI=false npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "14.21.3"
  NPM_FLAGS = "--legacy-peer-deps --force" 